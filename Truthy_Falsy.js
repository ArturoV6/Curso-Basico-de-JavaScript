//Ejemplos para valores falsos

Boolean(0); //false
Boolean(null); //false
Boolean(NaN); //false
Boolean(undefined); //false
Boolean(false); //false
Boolean(""); //false

//Ejemplos para valores verdaderos

Boolean(1); //true
Boolean(a); //true
Boolean([]); //true aunque este vacio
Boolean({}) //true aunque este vacio
Boolean(function(){}) //true
Boolean(true) //ture